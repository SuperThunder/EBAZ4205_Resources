#!/bin/sh
set -e
# Automatically added by dh_installinit
if ([ -x "/etc/init.d/connman" ] || [ -e "/etc/init/connman.conf" ]) && \
   [ "$1" = remove ]; then
	invoke-rc.d connman stop || exit $?
fi
# End automatically added section
